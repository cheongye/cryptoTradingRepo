spring.application.name=demo
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
# Set the logging level
logging.level.root=DEBUG

# Specify the log file
logging.file.name=app.log

# Or specify the log directory and file name
logging.file.path=logs
logging.file.name=application.log

#management.endpoints.web.exposure.include=*
#crypto.api.sources=BINANCE:https://api.binance.com/api/v3/ticker/bookTicker,HUOBI:https://api.huobi.pro/market/tickers
# Enable SQL logging
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

spring.datasource.initialization-mode=always
# Create or update the schema at startup, but do not drop existing tables
spring.jpa.hibernate.ddl-auto=update
spring.datasource.data=classpath:data.sql

 
crypto.api.sources.BINANCE=https://api.binance.com/api/v3/ticker/bookTicker
crypto.api.sources.HUOBI=https://api.huobi.pro/market/tickers
crypto.supported.symbols=ETHUSDT,BTCUSDT
crypto.symbol.USDT=USDT
crypto.symbol.currency=ETH,BTC,USDT


wallet.TestId=userOne

